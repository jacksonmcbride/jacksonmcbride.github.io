source 'https://rubygems.org'
gem 'jekyll'
gem "jekyll-sitemap"

# Required on Ruby 3.4+: these were removed from Ruby's default gems
# but Jekyll 4.3.3 still requires them internally.
gem 'csv'
gem 'logger'
gem 'base64'
gem 'bigdecimal'
